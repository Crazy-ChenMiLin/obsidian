1. 误区：以为"Entry就是key"
●     真相：Entry是包含key和value的容器，key只是Entry的一部分
2. 误区：以为"弱引用就不会内存泄漏"
●     真相：弱引用只解决了Key的内存泄漏，Value的内存泄漏需要开发者主动调用 remove() 来解决
3. 误区：以为"ThreadLocal自己存储了数据"
●     真相：数据实际存储在线程的ThreadLocalMap中，ThreadLocal只是提供访问接口


怎么做
	手动remove
	不存大对象因为大key
	是呀  static final

|     |                                             |                                                         |
| --- | ------------------------------------------- | ------------------------------------------------------- |
| 条件  | 具体行为                                        | 造成的结果                                                   |
| 前提  | 线程池核心线程长期存活，永远不被GC                          | 线程内部的ThreadLocalMap永久存在，不会被整体回收                         |
| 触发  | 业务代码把ThreadLocal的强引用置空（threadLocal = null）  | 除了Entry里的弱引用，没有其他引用指向ThreadLocal对象，GC时key被回收，变成null key |
| 核心  | 没有手动调用threadLocal.remove()，也没有其他途径访问这个Entry | 引用链永久存在：存活线程（GC Roots）→ ThreadLocalMap → Entry → value  |


❌ 错误句：
因为key在业务中改为null，但是value是强引用，无法gc回收，导致内存泄漏。

✅ 正确表述：
业务将ThreadLocal的引用变量置为null，导致ThreadLocal对象失去强引用被GC回收，Entry中的key变为null；
同时线程池线程长期存活（单个thread死亡后全部直接gc回收），Entry仍被Map强持有，value因强引用无法被GC回收，且无法被访问，最终造成内存泄漏。


currenthashmap和threadlocalmap的关系及其源码


currenthashmap出得很好题目