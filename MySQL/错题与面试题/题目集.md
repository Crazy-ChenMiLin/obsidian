### 第 2 题

```
start transaction;
truncate table student;
rollback;
```
truncate 是 DDL 语句，不支持事务

### 第 3 题


```
CREATE USER 'intern'@'%' IDENTIFIED BY 'Intern@123456';
```
`@'%'`表示**任意电脑都能远程登录**

### 第 4 题

```
GRANT SELECT, INSERT ON test_db.* TO 'intern'@'%';
```

```
REVOKE INSERT ON test_db.* FROM 'intern'@'%';
```
	撤销 intern 用户的 INSERT 权限

### 
### 第 7 题

```
mysql -uroot -p123456 school_db < D:/backup/school_20240601.sql
```
备份文件，还原

### 第 8 题

```
SHOW BINARY LOGS;
```

---

### 第 9 题
```
SELECT 
  ROW_NUMBER() OVER(PARTITION BY class_id ORDER BY score DESC) AS rank_num,
  student_name, class_id, score
FROM student_score;


SELECT 
  -- 核心：分组+排序+生成排名
  ROW_NUMBER() OVER(
    PARTITION BY 分组字段  -- 按什么维度分组（比如班级、部门）
    ORDER BY 排序字段 排序方式  -- 组内按什么排序（比如分数降序、业绩升序）
  ) AS 排名字段名,
  -- 以下是你要展示的业务字段
  字段1, 字段2, 字段3
FROM 数据表名;


```



```

---

### 第 10 题

WITH high_score_stu AS (
  SELECT student_id, student_name, score FROM student_score WHERE score >= 90
)
SELECT * FROM student s
JOIN high_score_stu h ON s.student_id = h.student_id;


#### 固定模板

-- 第一步：定义CTE（临时中间表），可以理解为“临时视图”
WITH 临时表别名 AS (
  -- 子查询：定义临时表的内容（筛选、聚合、关联都可以）
  SELECT 字段1, 字段2 FROM 数据表名 WHERE 筛选条件
)
-- 第二步：使用这个临时表做后续查询（关联、筛选、聚合等）
SELECT * FROM 主表 
JOIN 临时表别名 ON 主表.关联字段 = 临时表别名.关联字段;



```



## 1. 如何查询慢 sql
![[Pasted image 20260330205705.png]]

![[Pasted image 20260330205850.png]]

## 2. 如何具体的查看 sql 的性能
Eg: explain select * from user where name ="张三"。

1. 使用 explain
2. 观察 type 和 rows

|列名|小白版解读|怎么判断好坏？|
|---|---|---|
|**type**|SQL 的「查询等级」（最重要！）|👍 最好：const（极速）<br><br>⚠️ 及格：ref（正常）<br><br>❌ 最差：**ALL**（全表扫描 = 慢 SQL 元凶）|
|**rows**|扫描了多少行数据|数字**越小越好**！<br><br>查 1 行却扫描 10 万行 = 性能极差|
|**Extra**|额外警告|出现这 2 个就是大问题：<br><br>1. `Using filesort`（文件排序，极慢）<br><br>2. `Using temporary`（临时表，卡服务器）|
3. 如何优化 sql 性能


4. MySQL 记录 binlog 的方式主要包括三种模式？每种模式的优缺点是什么？


5.  事务介绍，分布式事物的理解，常见的解决方案有哪些，什么是两阶段提交、三阶段提交；



6. **什么是事务的 ACID？**
7. **MySQL 的默认隔离级别是什么？为什么选择它？**
8. **MVCC 如何实现可重复读？**
9. **幻读在可重复读级别下是否完全解决？如何解决？**
10. **RC 和 RR 在加锁上有何区别？**
11. **如何分析死锁？如何预防？