- 滑动窗口求的是**子序列**吗？
- 为什么哈希表需要**更新字符位置**？明明后面元素没有增加了
- 这个题目最长的不重复子串的**答案**是什么？
- 代码只是返回了**长度**，但是没有返回**具体的子串**是吧？
- Java 如何**遍历字符串里面每个元素**？
- 不懂为什么需要用到**当前窗口**这个东西和**窗口长度**
- `charLastIndex.get(c) >= left` 为什么需要这个判断？
- 重复的字符就在当前窗口，**为什么需要移动左指针**？
- `char c = s.charAt(right);` 可是 `c` 不是被定义为右指针吗？
- 那个 if 判断语句什么时候会触发？比如用 `aab` 举例
- `charLastIndex.put(c, right);` 这是干嘛的？