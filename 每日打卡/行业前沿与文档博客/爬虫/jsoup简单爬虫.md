# 静态页面

#### 1.  Jsoup 的核心能力

Jsoup 本质上只是一个**HTML 解析工具**，它的工作流程非常简单：

1. 向目标 URL 发送 HTTP 请求，获取网页的「原始 HTML 源码」（这一步和浏览器地址栏直接访问、然后右键 “查看网页源码” 看到的内容完全一致）；
2. 按照你写的 CSS 选择器 / 标签规则，从 HTML 源码里筛选、提取指定内容；
3. 它**没有任何执行 JavaScript 的能力**，也模拟不了浏览器渲染页面的过程。

