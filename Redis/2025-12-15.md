@Component 一般用在业务层、数据访问层这些地方，像 Service 类、Mapper 类。@Configuration 呢，主要是在配置文件里，比如要配置数据库连接、整合第三方框架的时候用。


# 全局唯一 key：
1. 秒杀场景下，如何保证每秒生成大量订单号不重复、不延迟？
2. Redis 宕机时，如何保证订单号正常生成且不丢失序列号？
3. 如何让订单号包含时间信息，同时又不会太长？
4. 多台服务器同时请求 Redis 生成序列号，会出现重复吗？
5. 如何让订单号既能区分日期，又能区分不同的业务线（如商品订单和服务订单）？
6. 生成的订单号被误删后，如何快速判断其是否真实存在，而不用频繁查询数据库？
7. 公司多套系统（如电商系统和物流系统）同时生成订单号，如何避免重复？


`System.currentTimeMillis()` 返回的是从 1970 年 1 月 1 日 00:00:00 UTC 到现在的毫秒数

`toEpochSecond()` 返回的是秒数。


Belike：
查询店铺 id。防止穿透

What：
优惠卷秒杀

Where：

数据库表：


### How:
When：
三层架构: 请求:

	If{
		时间到没到->mq决定
		查表
	}

When：
![[Pasted image 20251216115647.png]]
### 即将开始：

	优惠券写入缓存+生成全局唯一id
	Sql 写入100个缓存

### ==秒杀时间==到了：
	
	定时任务函数：请求开锁
其实是做的一个判断

### 抢优惠券：

	抢锁：

~~Static Fanal: 变量--~~

（写在数据库的 stock）


When：
### 使用中：
 ==一人一单问题：==
![[Pasted image 20251218182553.png]]
	抢到了时候：
		
~~存入个人的sql~~，放入缓存
~~每个用户都有独立的一张数据库~~
		更新数据库优惠卷的状态

用户 ID 和优惠券 ID 关联起来，存到数据库里。下次用户再抢的时候，先检查这个关联表

~~删除对应优惠卷的数据库sql~~

~~==分布式锁保证数据一致性==：抢优惠卷~~
用的乐观锁


==异步更新缓存提高响应速度==：缓存更新
## 存在的问题:

请求还是打数据库

### 秒杀时间过了：

	过期处理：
		1.MQ 监听适用于高并发且对时效性要求高的场景；
				Fanally{Mq 发信息。用完了删缓存 }
		2.Redis 过期通知机制适合对架构复杂度要求不高、数据量不是特别大的业务；
		3.定时任务扫描数据库常用于数据处理量不大、对实时性要求不太高，且追求实现简单的场景。
	请求锁住后续请求




### 付款时候：

		先查询个人的sql
		实际金额：减免/不减免

### 细节问题：
比如在抢锁、更新数据库这些操作失败的时候该怎么处理。还有就是可以增加一些监控报警，要是出现系统繁忙、缓存更新失败之类的情况，能及时发现。另外，对于恶意刷单的情况也可以想想应对策略，比如限制同一 IP 的抢购次数。


