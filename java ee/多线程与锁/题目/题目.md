# 多线程与锁 测试题目

## 一、选择题

1. 关于进程和线程的描述，下列说法正确的是：AB
   A. 进程是CPU调度的基本单位，线程是资源分配的基本单位(v)
   B. 线程共享进程的内存空间，进程之间相互隔离(V)
   C. 进程间通信只能通过Socket实现(X)
   D. 创建线程的开销比创建进程的开销大（X）

2. 以下哪种情况会导致线程进入BLOCKED状态：C
   A. 线程调用了Thread.sleep()
   B. 线程调用了Object.wait()
   C. 线程试图进入一个已被其他线程占用的synchronized块(V)
   D. 线程执行完毕

3. 关于synchronized和ReentrantLock的对比，下列说法错误的是：
   A. synchronized是隐式锁，ReentrantLock是显式锁 （V）对象
   B. 两者都支持可重入性
   C. ReentrantLock不支持公平锁
   D. ReentrantLock可以通过tryLock()尝试获取锁而不阻塞

4. 以下关于线程池的配置参数，说法正确的是： A  其他不知道
   A. corePoolSize是线程池能容纳的最大线程数(V)
   B. 使用无界队列时，maximumPoolSize参数不会生效 
   C. keepAliveTime是核心线程的空闲回收时间
   D. 线程池的拒绝策略只有AbortPolicy一种

5. 关于volatile关键字，下列说法正确的是：A
   A. volatile可以保证原子性
   B. volatile可以保证可见性和禁止指令重排 不知道
   C. volatile修饰的变量不需要其他同步措施 不知道
   D. volatile比synchronized性能差 不知道

## 二、简答题

1. 请简述Java中线程的六种状态及其转换条件。
 等待*2 block star  runing 完成
2. 请解释什么是线程的可重入性，并举例说明。
拿到锁的线程可以相较于其他线程再次抢到锁。 Sychronized 底层就有可重入性，偏向性


==在已经持有一把锁的情况下，**再次申请这把锁时，可以直接获取，不会被自己阻塞**。==



1. 请说明进程间通信的主要方式，以及线程间通信的主要方式。

==管道、命名管道、消息队列、共享内存、信号量、信号、Socket 套接字==



2. 请简述线程池的工作原理，以及如何合理配置线程池参数。
先自己初始化好多个线程，此时线程属于等待。有任务提交到线程池就触发调度


Completablefuture<string> 异步结果=completableFuture. Supplyasync (this：任务 1，asyncpool)


5. 请对比synchronized和ReentrantLock的优缺点，以及适用场景。

## 三、编程题




1. 请使用synchronized关键字实现一个线程安全的计数器，包含增加计数和获取当前计数的方法。

2. 请使用ReentrantLock实现一个线程安全的计数器，包含增加计数和获取当前计数的方法。

3. 请使用AtomicInteger实现一个线程安全的计数器。





4. 请创建一个线程池，配置核心线程数为4，最大线程数为20，使用有界队列，队列大小为1000，并实现优雅关闭。

5. 请使用CompletableFuture实现一个异步任务链：首先执行一个耗时操作，然后对结果进行转换，最后处理可能的异常。

 # 四、分析题

1. 分析以下代码可能存在的问题，并说明如何修复：


public class Counter {
    private ~~int~~ count = 0;
    ==Private atomicInteger count =new atomicinteger (0);==



    public sychronized void increment() {
        count++;
    }
    
    public int getCount() {
        return count;
    }
}

2. 分析以下线程池配置的问题：

```java
ExecutorService pool = Executors.newFixedThreadPool(10);
```

过于单一，应该配置线程池的初始化



3. 请分析线程状态转换：当线程调用了Object.wait()方法后，它会进入什么状态？当其他线程调用了notify()方法后，它会如何变化？
Waiting 状态（要使用 while（true）包装一直 wait）。会去抢线程/锁。

## 五、扩展题

1. 请简述分布式锁的使用场景和实现方式。




2. 请说明wait/notify和notifyAll的区别，以及使用时的注意事项。

3. 请简述并发调试和性能优化的常用工具和方法。