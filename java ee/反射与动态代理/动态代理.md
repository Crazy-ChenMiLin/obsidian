接口是方法-->工厂-->放入请求

方法返回值声明为接口，本质是 **“面向接口编程”** 的编码实践，核心目的是**解耦**，和动态代理没有直接关联。

1. 对于 JDK 动态代理：它的技术特性决定了**必须基于接口生成代理对象**，因此在使用 JDK 动态代理时，方法的返回值类型通常会声明为该接口（比如你 LangChain 4 j 的代码）—— 这是动态代理的 “表现形式”，但不是 “判定依据”。
2. 对于 CGLIB 动态代理：它无需依赖接口，可直接代理普通具体类，此时方法返回值可以是具体类类型，也可以是接口类型（若目标类实现了接口）。
