
| 继续锁的 sychronised | 昨天呜，跟学姐聊天+看 z 30 的讲解处理 | √   |     |
| ---------------- | ---------------------- | --- | --- |
| 理解项目的架构          |                        |     |     |
|                  |                        |     |     |
|                  |                        |     |     |
|                  |                        |     |     |
|                  |                        |     |     |

# 一.引入
之前黑马点评期末的时候用了一个·，Redission 的分布式锁，跨 jvm 的锁.
发现锁的内容比较多：多线程-资源-io-选择的判断
自己的项目有 synchronise

因为刚好在看 jvm 的原理，整理一下。关于锁的升级
# 锁的升级
无锁

偏向锁
轻量级锁
重量级锁
![[Pasted image 20260201142056.png]]
# Java 主流锁类型对比表

| 分类维度         | 锁名称      | 核心思想                             | 适用场景              | Java 示例代码                                                                                                                       |
| ------------ | -------- | -------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| **是否锁住同步资源** | 悲观锁      | 假设一定会有并发冲突，先加锁再操作。               | 写操作频繁、冲突概率高的场景    | public synchronized void decreaseStock() { ... }<br>`                                                                           |
|              | 乐观锁      | 假设并发冲突很少发生，不加锁，仅在更新时用 CAS 检查。    | 读多写少、冲突概率低的场景     | `<br>AtomicInteger count = new AtomicInteger(0);<br>count.incrementAndGet();<br>                                                |
| **锁失败后是否阻塞** | 阻塞锁      | 获取锁失败则挂起线程，进入等待队列。               | 锁持有时间较长的场景        | <br>synchronized (this) { ... }<br>                                                                                             |
|              | 自旋锁      | 获取锁失败不阻塞，循环重试（自旋）。               | 锁持有时间极短的场景        | <br>AtomicReference<Thread> lock = new AtomicReference<>();<br>while (!lock.compareAndSet(null, Thread.currentThread())) {}<br> |
|              | 适应性自旋锁   | 自旋次数动态调整，根据历史竞争情况优化自旋时间。         | JVM 自动优化场景        | Java 6+ 中 `synchronized` 轻量级锁竞争时的自适应自旋（无需代码显式控制）                                                                                |
| **竞争流程细节**   | 无锁       | 完全不用锁，依赖 CAS 保证原子性，失败线程自动重试。     | 高并发读、写冲突极少的场景     | `java<br>AtomicLong idGenerator = new AtomicLong(0);<br>idGenerator.getAndIncrement();<br>`                                     |
|              | 偏向锁      | 锁会 “偏向” 第一个获取它的线程，后续该线程访问无需额外检查。 | 单线程频繁访问同步资源的场景    | Java 6+ 中 `synchronized` 默认开启偏向锁（可通过 JVM 参数关闭）                                                                                  |
|              | 轻量级锁     | 多线程竞争时，未获取锁的线程自旋等待，不阻塞。          | 多线程交替访问、锁持有时间短    | Java 6+ 中 `synchronized` 竞争升级为轻量级锁阶段                                                                                            |
|              | 重量级锁     | 多线程竞争时，未获取锁的线程被阻塞挂起，进入操作系统内核态等待。 | 多线程激烈竞争、锁持有时间长    | Java 6+ 中 `synchronized` 竞争升级为重量级锁阶段                                                                                            |
| **竞争时是否排队**  | 公平锁      | 严格按线程请求顺序排队，先到先得，不允许插队。          | 需要避免线程饥饿的场景       | Lock fairLock = new ReentrantLock(true);<br>fairLock.lock();<br>                                                                |
|              | 非公平锁     | 允许插队，刚释放锁的线程可再次尝试获取，提高锁利用率。      | 高并发、追求性能的场景       | Lock unfairLock = new ReentrantLock(); // 默认非公平<br>unfairLock.lock();<br>                                                       |
| **是否可重入**    | 可重入锁     | 同一个线程可多次获取同一把锁，不会死锁。             | 递归调用、多层同步场景       | public synchronized void recursive() { recursive(); }<br>                                                                       |
|              | 非可重入锁    | 同一个线程不能多次获取同一把锁，否则会发生死锁。         | 简单互斥场景（实际开发中极少使用） | 自定义简单自旋锁（未记录持有线程，再次获取会阻塞自己）                                                                                                     |
| **是否可共享**    | 共享锁      | 允许多个线程同时持有锁，适合读操作。               | 读多写少的场景（如缓存）      | ReadWriteLock rwLock = new ReentrantReadWriteLock();<br>Lock readLock = rwLock.readLock();<br>readLock.lock();<br>              |
|              | 排他锁（独占锁） | 同一时间仅允许一个线程持有锁，适合写操作。            | 写操作频繁、数据一致性要求高    | Lock writeLock = rwLock.writeLock();<br>writeLock.lock();<br>                                                                   |

---
Synchronized 的特性比较多
讲讲升级

# 一. synchronized发生的位置 where
 ![[Pasted image 20260129120610.png]]

- 解释器是 “逐行解释执行”，启动快但执行慢。
- JIT 是在运行时识别 “热点代码”（频繁执行的代码），将其编译成机器码并缓存，后续再执行时直接用缓存的机器码，大幅提升效率。

简单来说：
- `javac` 负责 **静态编译**（`.java` → `.class`）
- 解释器负责 **动态解释执行**（`.class` → 机器码，逐行）
- JIT 负责 **动态编译优化**（热点 `.class` → 机器码，缓存）



# 二.为什么执行引擎操作锁的升级？？

### 官网描述：执行引擎提供锁的服务

	即：执行引擎不止编译执行代码. 锁也归它管
	如下：

![[Pasted image 20260129135723.png]]


#  

### 大概解释：

![[1ab7414bf122744dce8d7f530eb636f2.png]]

### 详细解释：
四. 锁升级发生的场景 when
#### **偏向锁（Biased Locking）**

-- **适用场景**：只有==一个线程==访问 syn 的情况
- **原理**：
    1. 当第一个线程访问同步块并获取锁时，会在 Mark Word 中记录该线程的 ID（偏向线程 ID）。
    2. 后续该线程再次进入同步块时，无需任何同步操作，直接获取锁，效率极高。
- **撤销条件**：当有其他线程尝试竞争偏向锁时，持有偏向锁的线程会被挂起，偏向锁升级为轻量级锁。
#### **轻量级锁（Lightweight Lock）**
- **适用场景**：多个线程交替==访问同步块==，无实际竞争
- **原理**：
    1. 线程进入同步块前，JVM 会在当前线程的栈帧中创建一个锁记录（Lock Record）。
    2. 通过 CAS（Compare and Swap）操作尝试将 Mark Word 更新为指向锁记录的指针：
        - 成功：获取轻量级锁。CPU 资源占用
        - 失败：表示有其他线程竞争，锁升级为重量级锁。
#### **重量级锁（Heavyweight Lock）**
- **适用场景**：多个线程同时==竞争==锁
- **原理**：
    1. 锁升级为重量级锁后，Mark Word 存储指向 Monitor 的指针。
    2. 未获取到锁的线程会被阻塞（进入 Monitor 的 EntryList），释放 CPU 资源。
    3. 锁释放时，会唤醒 EntryList 中的线程重新竞争。
    4. 
- ==**缺点**：线程阻塞和唤醒涉及用户态与内核态的切换，性能开销大。==


Hotspot 是 j 家的一个 jvm

- ![[Pasted image 20260129133036.png]]

### 文字版：

![[Pasted image 20260129133930.png]]

### **总结**
Jvm

执行引擎 干了：
`synchronized` 的核心原理是基于 ==Monitor 指针==（重锁的排队争取）和==对象头==（锁的切换）的锁机制，
通过锁升级策略（偏向锁 → 轻量级锁 → 重量级锁）在不同场景下   平衡性能和线程安全


| 业务场景                | 推荐分布式锁实现                | 实现方法                                                             |
| ------------------- | ----------------------- | ---------------------------------------------------------------- |
| **高并发高性能**，非严格要求公平锁 | Redis 分布式锁（推荐 Redisson） | ==Redis 的 SET NX EX 指令==或者 ==lua==  或者 ==java 自带的原子性命令 atom== 等等 |
| 严格要求**公平锁**，高可用     | ZooKeeper 分布式锁          | 基于临时顺序节点实现，天然支持公平锁，故障时临时节点自动释放；适合分布式任务调度、分布式选主、跨节点的有序任务执行        |

