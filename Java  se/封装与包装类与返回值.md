# 一.封装类 Integer和数据类型 int的关系
### 1. **定义与本质**

- **基本数据类型**：是编程语言提供的最基础的数据形式，用于存储简单的数值或字符
- 例如 Java 中的`int`（整数）、`double`（浮点数）、`char`（字符）等。它们不是对象，不具备对象的特性（如方法、属性），仅用于存储数据。
- **封装类（包装类）**：是对基本数据类型的 “包装”，将基本数据类型封装为**对象**，使其具备对象的特性（可以调用方法、参与面向对象的操作）。
- 例如 `int` 的封装类是 `Integer`，`double` 的封装类是 `Double` 等。
例如：
- **装箱（Boxing）**：将基本数据类型转换为对应的封装类对象。
    `int num = 10; Integer obj = Integer.valueOf(num);`
- **拆箱（Unboxing）**：将封装类对象转换为对应的基本数据类型。
    `Integer obj = new Integer(10); int num = obj.intValue();

| 基本类型    | 对应包装类     | 手动装箱核心方法（静态）               | 手动拆箱核心方法（实例）   | 自动装箱底层调用                 | 自动拆箱底层调用       | 备注                   |
| ------- | --------- | -------------------------- | -------------- | ------------------------ | -------------- | -------------------- |
| byte    | Byte      | Byte.valueOf(byte b)       | byteValue()    | Byte.valueOf(byte)       | byteValue()    | 缓存 - 128~127         |
| short   | Short     | Short.valueOf(short s)     | shortValue()   | Short.valueOf(short)     | shortValue()   | 缓存 - 128~127         |
| int     | Integer   | Integer.valueOf(int i)     | intValue()     | Integer.valueOf(int)     | intValue()     | 缓存 - 128~127（可配置）    |
| long    | Long      | Long.valueOf(long l)       | longValue()    | Long.valueOf(long)       | longValue()    | 缓存 - 128~127         |
| float   | Float     | Float.valueOf(float f)     | floatValue()   | Float.valueOf(float)     | floatValue()   | 无缓存（浮点型）             |
| double  | Double    | Double.valueOf(double d)   | doubleValue()  | Double.valueOf(double)   | doubleValue()  | 无缓存（浮点型）             |
| char    | Character | Character.valueOf(char c)  | charValue()    | Character.valueOf(char)  | charValue()    | 缓存 \u0000~\uffff（所有） |
| boolean | Boolean   | Boolean.valueOf(boolean b) | booleanValue() | Boolean.valueOf(boolean) | booleanValue() | 缓存 TRUE/FALSE 单例     |
# 二.nteger 的底层核心就是private int ` 

```
public final class Integer {
    // 底层用private int存储实际数值，外部无法直接访问
    private final int value;

    // 构造方法：初始化这个private int
    public Integer(int value) {
        this.value = value;
    }

    // 提供public方法让外部获取这个值（拆箱）
    public int intValue() {
        return value;
    }
}
```


### [
# 泛型擦除

哪些地方可能被擦除？
擦除后变成 object

