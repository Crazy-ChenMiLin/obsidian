#     1.“接口” 本身就是 “抽象接口”
`public interface CategoryService` 
本质上和
“`public abstract interface CategoryService`” 是等价的

# 接口契约
约定了：
==方法参数类型==、==返回值类型==以及==方法调用时机==

```java
public interface HandlerInterceptor {
    default boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
        return true;
    }
    default void postHandle(HttpServletRequest request, HttpServletResponse response, Object handler, @Nullable ModelAndView mv) throws Exception {}
    default void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, @Nullable Exception ex) throws Exception {}
}
```





# ![[Pasted image 20260131141159.png]] `new ArrayList<>()` 是 `new ArrayList<String>()` 的 “简化版”（Java 7+），只要写法规范，存储的元素类型完全一致。



# Arraylist 也可以实现其他接口如 queue.
``` java
class MyStack {
    Queue<Integer> queue1;
    Queue<Integer> queue2;
    public MyStack() {
        queue1 = new LinkedList<>();
        queue2 = new LinkedList<>();
    }
```

# 泛型与没有泛型

| 特性      | `List<String> listRole = new LinkedList<>();`（带泛型） | `List listRole = new LinkedList();`（无泛型，原始类型） |
| ------- | -------------------------------------------------- | --------------------------------------------- |
| 元素类型限制  | 只能存储`String`类型，其他类型（int、Boolean 等）编译报错             | 可存储**任意类型**（String、int、Object 等），无限制          |
| 编译期类型检查 | 编译器主动拦截错误（比如存 int），保证类型安全                          | 编译器不做类型检查，错误只会在运行时暴露                          |
| 取值方式    | 直接取 String，无需强制类型转换                                | 取值是 Object 类型，必须手动强转，容易出错                     |
| 类型安全    | 安全（编译期避免类型错误）                                      | 不安全（运行时可能抛出 ClassCastException）               |
| 开发推荐度   | 强烈推荐（符合现代 Java 规范）                                 | 绝对不推荐（原始类型，失去泛型的核心价值）                         |
