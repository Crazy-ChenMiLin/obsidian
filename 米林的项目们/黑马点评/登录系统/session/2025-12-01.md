```java
    // 模拟：登录成功后，处理用户信息的逻辑
    public void handleAfterLogin(User user, HttpSession session) {
        // 1. 存东西：把用户信息放进session小柜子（对应 setAttribute）
        // 标签名："loginUser"（自己起的，取的时候要和这个一致）
        // 要存的东西：把User转换成UserDTO（你项目里常用的用户数据传输对象）
        UserDTO userDTO = convertUserToDTO(user); // 模拟转换（不用管具体实现）
        session.setAttribute("loginUser", userDTO); 
        System.out.println("✅ 完成「存东西」：把用户信息贴标签\"loginUser\"，放进session小柜子");

        // 2. 取东西：从session小柜子里取出刚才存的用户信息（对应 getAttribute）
        // 必须用同一个标签名"loginUser"，才能取到
        UserDTO gettedUserDTO = (UserDTO) session.getAttribute("loginUser");
        System.out.println("✅ 完成「取东西」：按标签\"loginUser\"，从session小柜子取出用户信息");

        // 3. 写东西：把取出来的用户信息，记到日志里（对应 log.debug）
        // 日志模板："当前登录用户：{}"（{}是空位）
        // 要写的内容：gettedUserDTO（刚才取出来的用户信息）
        log.debug("✅ 完成「写东西」：当前登录用户：{}", gettedUserDTO);
    }

    // 模拟：把User转换成UserDTO（不用关注这个方法的实现，只是为了代码完整）
    private UserDTO convertUserToDTO(User user) {
        UserDTO dto = new UserDTO();
        dto.setId(user.getId());
        dto.setNickName(user.getNickName());
        dto.setPhone(user.getPhone());
        return dto;
    }
}
```


Session 是直接导入的

- session 的作用：单服务器下如何保持用户登录状态；
- 分布式系统的基本场景：多服务器集群部署时，请求会分发到不同服务器；
- Redis 基础：键值对存储、过期时间设置、字符串类型的基本操作；
- 短信登录的常规流程：获取验证码→提交验证码→登录成功。

## 各小节预习要点

### P31 实战篇 - 08. 短信登录 - session 共享的问题分析

1. 回忆：单服务器环境中，session 是怎么和用户登录状态绑定的？
2. 思考：如果项目部署了多台服务器（集群），用户请求分到不同服务器时，session 为什么会 “不共享”？

基于 session 不共享，相互拷贝数据耗费内存空间。
得换存储方法到数据库

改动在哪

手动有效期

Ppt

登录验证的 redis:
转化 user 对象为 hash 结构

保存用户信息：
生成 token

用户存入 redis: 数据结构

用户有效期：


返回 token



### P32 实战篇 - 09. 短信登录 - Redis 代替 session 的业务流程

1. 对比：Redis 相比 session，在分布式场景下有哪些天然优势？
2. 梳理：用 Redis 存登录状态的话，从 “用户发验证码” 到 “登录成功”，需要在 Redis 中存哪些信息？
3. 思考：登录成功后，Redis 里的登录信息该用什么 “标识”（比如键名）和用户绑定？
用手机号和验证码为 key

为什么是 hash 而不是 string?
内存小



# 什么是硬通货

Session
Token

### P33 实战篇 - 10. 短信登录 - 基于 Redis 实现短信登录

1. 复习：Redis 中 “存验证码”“存用户信息” 对应的命令（比如`set key value EX 过期时间`）；
2. 设计：存验证码时，键名可以怎么设计（比如加前缀区分业务，如`code:手机号`）？
3. 预判：验证验证码时，要做哪些判断（比如验证码是否存在、是否正确）？

### P34 实战篇 - 11. 短信登录 - 解决状态登录刷新的问题

1. 思考：Redis 中登录信息设置了过期时间，用户活跃时怎么避免 “没到时间就过期，需要重登”？
2. 预判：在请求拦截环节，如何判断是否要 “刷新” Redis 中登录信息的过期时间？

