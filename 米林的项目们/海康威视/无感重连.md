# 二. 新连接
旧的关闭后(网页刷新后)，新连接进来（再次打开摄像头）
如果设备正忙
睡会再进来
``` java
// 代码片段示意  
while (retryCount < 5) {  
    nRet = OpenDevice();  
    if (nRet == 设备正忙) {  
         // 嘿，旧的连接还没退出去，我等它 1.5 秒  
         Thread.sleep(1500);  
         retryCount++; // 再试一次  
    }  
}
```