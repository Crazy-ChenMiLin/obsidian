# 线程通信：TPC 为例
![[Pasted image 20260415213010.png]]


## TCP 为什么不够好？（不考虑 tcp 未加密的情况）

[[不足]]


# 多线程通信

线程之间通信的方案选择
	Hashmap------->currenthashmap
	共享变量




# BIO 到 NIO 的演变






## NIO 的核心设计
![[产出 2026-04-15 20.11.49.excalidraw]]
## 代码


Windows 中 NIO 的底层是：以 windows 举例
![[产出 2026-04-15 20.07.43.excalidraw]]

- 在**Windows**上（Java 8）会显示：`sun.nio.ch.WindowsSelectorImpl`
- 在**Windows**上（Java 9+）会显示：`sun.nio.ch.WEPollSelectorImpl`
	- **W**indows **E**vent

|平台|1000 个连接|10000 个连接|适合场景|
|---|---|---|---|
|Linux (epoll)|✅ 轻松|✅ 轻松|高并发服务器|
|Windows (select)|⚠️ 还行|❌ 吃力|普通应用|
|Windows (Java 9+ WEPoll)|✅ 还行|⚠️ 一般|中等并发|
# NIO 的实现 netty


~~具体的 servers 创建的代码：借鉴于反射的 hander 代理对象：~~封装的思想

- 动态代理：`Proxy.newProxyInstance` 搭台
- Netty：`启动器` 搭台

- **具体干活的逻辑，全部抽出来，封装进一个「Handler」里**
    动态代理：逻辑写在 `handler.invoke()`
    Netty：逻辑写在 `MyHandler.channelRead()`

```java
// 3. 配置核心参数
bootstrap.group(bossGroup, workerGroup) // 绑定主从线程组
         .channel(NioServerSocketChannel.class) // 指定NIO通道类型
         .option(ChannelOption.SO_BACKLOG, 128) // 连接队列大小
         .childOption(ChannelOption.SO_KEEPALIVE, true) // 心跳保活
         // 4. 初始化管道，添加自定义处理器
         .childHandler(new ChannelInitializer<SocketChannel>() {
             @Override
             protected void initChannel(SocketChannel ch) {
                 // 向Pipeline添加自定义Handler（业务逻辑）
                 ch.pipeline().addLast(new MyServerHandler());
             }
         });

```

```java
//反射的代理类
// 动态代理演示。  
        // 创建动态代理调用处理器。  
        SetterLoggingInvocationHandler handler = new SetterLoggingInvocationHandler(realPerson);  
        // 基于 PersonApi 接口创建动态代理对象。  
        PersonApi dynamicProxy = (PersonApi) Proxy.newProxyInstance(  
                PersonApi.class.getClassLoader(),  
                new Class<?>[]{PersonApi.class},  
                handler  
        );
```



# 主流 RPC 框架的 NIO 实现

- **Dubbo**：默认使用**Netty**作为底层通信框架，基于 NIO 实现 TCP 长连接通信，采用 Reactor 主从多线程模型：
    - **BossGroup**：主 Reactor 线程组，负责处理 TCP 连接请求
    - **WorkerGroup**：从 Reactor 线程组，负责处理已建立连接的 IO 读写事件
- **gRPC**：基于 HTTP/2 协议，底层使用**Netty**或**OkHttp**实现，同样采用 NIO 模型，支持多路复用和头部压缩。
    
- **SOFARPC**：蚂蚁集团开源框架，**完全基于 Netty**构建，通过封装 TCP 长连接实现高性能远程调用。
    
- **Hadoop RPC**：直接基于**Java NIO**实现，使用 Protocol Buffers 进行序列化，消息头包含 32 位调用 ID 和 16 位操作码。


